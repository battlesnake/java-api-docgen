apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'eclipse'

apply from: 'common-build.gradle'

group = 'com.opencosmos'
version = '0.2'

description = """"""

sourceCompatibility = 8
targetCompatibility = 8

dependencies {
	testCompile 'junit:junit:4.12'
}

sourceSets {
	main {
		java {
			srcDir "src/main/java"
		}
	}
}
